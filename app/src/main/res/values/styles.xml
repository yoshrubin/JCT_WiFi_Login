<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/md_blue_500</item>
        <item name="colorPrimaryDark">@color/md_blue_700</item>
        <item name="colorAccent">@color/accent_material_light</item>
        <item name="android:windowBackground">@color/background_material_light</item>
        <item name="android:textColorPrimary">@color/primary_text_default_material_light</item>
        <item name="android:textColorSecondary">@color/secondary_text_default_material_light</item>
        <item name="android:buttonStyle">@style/button</item>

        <!-- API < 21 -->
        <item name="android:editTextColor">@color/primary_text_default_material_light</item>
        <!-- API > 21 -->
        <item name="editTextColor">@color/primary_text_default_material_light</item>
    </style>
    <style name="button" parent="Widget.AppCompat.Button">
        <item name="android:background">@color/md_blue_500</item>
        <item name="android:textColor">@color/primary_text_default_material_dark</item> <!-- Here is specified text color -->
    </style>
</resources>
